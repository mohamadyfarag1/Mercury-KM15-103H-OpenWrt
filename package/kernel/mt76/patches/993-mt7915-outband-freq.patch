--- a/mt7915/mcu.c
+++ b/mt7915/mcu.c
@@ -2824,6 +2824,7 @@
 		.tx_path_num = hweight16(phy->mt76->chainmask),
 		.rx_path = mt7915_band_chainmask(phy),
 		.band_idx = band,
 		.channel_band = ch_band[chandef->chan->band],
+		.outband_freq = cpu_to_le32(freq1),
 	};
 
 #ifdef CONFIG_NL80211_TESTMODE
